'use strict';

const _ = require('underscore');

const DeviceDriver = require('node-homey-devicedriver');

class WifiDriver extends DeviceDriver {

	constructor(driverId, options) {
		super(driverId, options);
	}
}

module.exports = WifiDriver;
